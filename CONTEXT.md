# JustSayIt 领域上下文

## 核心体验

JustSayIt 的核心体验是：用户用自然语言说一句话，AI 理解后完成记账，用户不需要填写传统表单。

## 数据与隐私词汇

- **本地优先**：账本的主副本由用户设备保存和掌控；本地设备是账本的主要工作场所。
- **账本主副本**：用户设备上的账本数据，是应用日常读取和写入的主要来源。
- **可选同步**：用户可以把账本同步到自己控制的 Google Drive，作为跨设备访问和恢复用的副本；这不是 JustSayIt 自己的云端账本。
- **当次 AI 处理**：用户输入的原始文字或语音可能在当次记账时经过服务端并转给 AI，用于理解和结构化；这不等于 JustSayIt 保存用户的完整账本。
- **不保存账本**：JustSayIt 的服务端不建立用户完整账本的持久副本。对外表达应使用这个具体事实，而不是笼统承诺“绝对安全”。

## 对外表达边界

推荐的核心主张：

> 本地优先，账本主副本在你的设备。

推荐的易懂解释：

> AI 只负责理解你说的话，账本由你的设备保存。需要跨设备时，可以同步到你自己的 Google Drive，而不是存进 JustSayIt 的云端账本。

避免使用：

- “数据完全存储在本地”（开启 Google Drive 同步时不再是字面上的完全本地）
- “服务器完全不知道你花了什么钱”（当次 AI 结构化会经过服务端）
- “100% 安全”“绝对不会泄露”等无法由系统结构保证的绝对承诺
- 仅使用“隐私安全”“数据属于你”等缺少存储位置说明的泛化表述

## 目标用户

首要目标用户是关心隐私、但不熟悉技术术语的普通用户。首屏应该解释“数据在哪里”，而不是解释 IndexedDB、端到端加密或其他实现细节。
