# entrypoint.sh/backup.sh run inside Linux containers — a CRLF shebang
# line breaks `/bin/sh` on checkout from a Windows client with
# core.autocrlf=true. Force LF regardless of platform checkout settings.
*.sh text eol=lf
